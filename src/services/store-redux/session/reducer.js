// Начальное состояние
const initialState = {

}

// Обработчик действий
function reducer(state = initialState, action) {
    switch(action.type) {
        default:
            // Нет изменений
            return state;
    }
}

export default reducer;